# Todo List на React

Простое и современное веб-приложение для управления списком задач, созданное с использованием React.

## 🚀 Функциональность

Приложение предоставляет следующий базовый набор функций:

*   **Добавление задач**: Введите текст задачи в поле ввода и нажмите Enter или кнопку "Добавить".
*   **Удаление задач**: Удалите любую задачу, нажав на соответствующую кнопку (например, корзину или крестик) рядом с ней.
*   **Отметка как выполненной**: Отмечайте задачи как выполненные (зачеркивание текста или изменение стиля).
*   **Локальное сохранение**: Список задач автоматически сохраняется в локальное хранилище браузера (LocalStorage) и восстанавливается при повторном посещении страницы.

## 🛠 Технологии

*   **HTML5 (JSX)**: Семантическая разметка.
*   **SCSS(CSS3)**: Стилизация, Flexbox/Grid для верстки, плавные анимации.
*   **React**: Чистая реализация логики приложения, обработка событий, использование LocalStorage.

## 📦 Установка и запуск

Чтобы запустить проект локально на своем компьютере, выполните следующие шаги:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Artush3/To-Do-List-React.git
    ```
2.  **Перейдите в папку проекта:**
    ```bash
    cd To-Do-List-React
    ```
3.  **Установите зависимости:**
    ```bash
    npm install
    ```
4.  **Запустите локальный backend:**
    ```bash
    npm run server
    ```
5.  **Запустите dev-сервер:**
    ```bash
    npm run dev
    ```